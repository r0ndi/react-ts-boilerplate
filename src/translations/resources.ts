import common from "./resources/en/common.json";
import messages from "./resources/en/messages.json";

const resources = {
    en: {
        translation: {...common, ...messages}
    },
};

export default resources;
